# assets

Small, deliberate fixtures: the generated sample character and the reference
outputs that visual regression checks compare against.

Nothing large or generated-on-demand belongs here. Exported animations,
rendered previews and build output stay out of version control.
